jdk: openjdk17
before_install:
  - echo "signing.key=${{ secrets.SIGNING_KEY }}" >> local.properties && echo "signing.password=${{ secrets.SIGNING_PASSWORD }}" >> local.properties && echo "signing.keyId=${{ secrets.SIGNING_KEY_ID }}" >> local.properties